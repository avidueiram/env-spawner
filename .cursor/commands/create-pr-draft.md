# Pull Request Draft

Follow instructions defined for command `/create-pr` in `.cursor/commands/create-pr.md`.

The only difference is that the pull request should be created as a draft instead of ready for review.
