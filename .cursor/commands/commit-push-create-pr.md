# Commit + Push + Create PR

Follow instructions defined for command `/commit-push` in `.cursor/commands/commit-push.md`.

After committing, push to the current branch.

Finally, create a pull request using the command `/create-pr` in `.cursor/commands/create-pr.md` going from the current branch to master.
